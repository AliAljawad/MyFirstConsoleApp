namespace MyFirstConsoleApp
{
    public class Laptop : BaseProduct
    {
        public Laptop(int id, string? brand, string? name, string? color, decimal? cost, decimal? price) : base(id, brand, name, color, cost, price) { }

    }
}
